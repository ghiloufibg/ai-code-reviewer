apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: ai-reviewer
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: http
      nodePort: 30088
      protocol: TCP
      name: http
